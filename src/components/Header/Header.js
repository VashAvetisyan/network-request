import React from 'react'

import './Header.scss'

const Header = () => {
    return (
        <div className="app-header">
            <nav>
                <h1 className="app-header__title">Network Requesta</h1>
            </nav>
        </div>
    )
}

export default Header
